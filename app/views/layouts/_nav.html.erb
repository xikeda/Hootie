<!-- We will render two seperate Navbars depending on whether or not user is signed in-->

<% if !user_signed_in? %>
  <%= render '/layouts/nav_guest' %>
<% else %> 
  <%= render '/layouts/nav_user' %>
<% end %>

